{
  "name": "mapdown-ocr",
  "version": "1.0.0",
  "description": "an ocr application of MapDown Web App.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "browser-sync start --server --https --files \"*.css, *.html, *.js\"",
    "build-css": "tailwindcss build style.css -o public/style.css"
  },
  "author": "BenThomson",
  "license": "MIT",
  "dependencies": {
    "browser-sync": "^2.26.13"
  }
}
